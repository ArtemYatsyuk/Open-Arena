/**
 * Smoke test placeholder. Replaced in Phase 1 with real backend tests
 * (auth, chat SSE, admin) once supertest is wired up.
 */
import { describe, it, expect } from 'vitest';

describe('backend smoke', () => {
  it('runs the test harness', () => {
    expect(1 + 1).toBe(2);
  });
});
